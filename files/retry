cache_regions=True,       # évite la détection de région à chaque nouveau bucket
    version_aware=False,      # évite le listing complet des versions (défaut normalement)
    skip_instance_cache=False
