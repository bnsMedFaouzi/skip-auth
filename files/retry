import pytest
from unittest.mock import MagicMock, patch


@pytest.fixture
def cft_app():
    """Bypasses __init__ to avoid all dependency initialization."""
    from data_push_cft.output_platform.cft.application import Cft
    app = Cft.__new__(Cft)
    app._client = MagicMock()
    app._consumer = MagicMock()
    app._producer = MagicMock()
    app._file_manager = MagicMock()
    return app


@pytest.fixture
def mock_file_info():
    file_info = MagicMock()
    file_info.path = "bucket/subdir/file.txt"
    file_info.model_dump.return_value = {"name": "file.txt", "path": "bucket/subdir/file.txt"}
    return file_info


@pytest.fixture
def mock_cft_flow():
    return MagicMock()
